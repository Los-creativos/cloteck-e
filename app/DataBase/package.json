{
  "name": "db",
  "version": "0.0.1",
  "description": "The data base model for the e-commerce",
  "author": {
    "name": "Los creativos"
  },
  "license": "ISC",
  "private": false,
  "scripts": {
    "dev": " docker compose -f docker-compose-db.yml down --rmi all &&  docker volume rm --force database_pg-data && docker volume rm --force database_pgadmin-data && docker compose -f docker-compose-db.yml up -d "
  }
}
